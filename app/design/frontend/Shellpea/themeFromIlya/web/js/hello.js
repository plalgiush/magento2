define([
    'jquery'
], function ($) {
    'use strict';

    return function (config, element) {
        console.log(config);
        console.log(element);
        console.log("hello world");
    }
});


